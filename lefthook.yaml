pre-commit:
  parallel: false
  exclude:
    - .pixi/
  commands:
    fmt:
      run: pixi run fmt

    lint:
      run: pixi run lint

    types:
      run: pixi run types

    imports:
      run: pixi run imports

    test:
      run: pixi run test